apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: airflow-local-dags-folder
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi      